/** Stamped into `interop.format` on every node, and part of every derived id.
 *  Its own module so the adapter can read it without importing the profile
 *  that builds the adapter. */
export const IFC_FORMAT = "ifc4";
